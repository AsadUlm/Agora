# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Settings / env
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# LLM providers
groq>=0.9.0          # Groq async SDK  (LLM_PROVIDER=groq)
openai>=1.30.0       # OpenAI async SDK (LLM_PROVIDER=openai)
httpx>=0.27.0        # Shared async HTTP client used by both SDKs

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
aiosqlite>=0.20.0   # Async SQLite driver for test DB
